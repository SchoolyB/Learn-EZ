import @std
import @arrays

do main(){
    temp dynamicArr [int] = {1,2,3}
    const fixedArr [int, 3] = {1,2,3}
    temp names [string] = {"John", "Paul", "Sarah", "Kyle"}

    temp matrix [[int]] = {{1,2,3},{7,8,9}}

    for_each m in matrix {
        std.println(m)
    }
}

